import 'package:flutter/material.dart';

class PickseatScreen extends StatelessWidget {
  const PickseatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('chon ghe'));
  }
}